.container.mt-5
  %h1.text-secondary.text-center.mb-5
    %span.text-primary Your tickets

  - @tickets.each do |ticket|
    .row
      .col-4
        %h3
          Ticket number:
          = ticket.id
      .col-4
        %h4.text-secondary
          Booked At:
          = ticket.updated_at.strftime("%a, %B %d %T")

